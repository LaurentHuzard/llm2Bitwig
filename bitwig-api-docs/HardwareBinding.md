# Interface HardwareBinding

Represents a binding from some hardware input to a target. When the binding is no longer needed the removeBinding() method can be called to remove it.

## Method Details

### `removeBinding`

```java
void removeBinding()
```

Removes this binding between its source and target so it is no longer in effect.

---

